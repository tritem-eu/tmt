if (app.userActivity.isToReplace()){
  app.userActivity.replaceWith("<%= j(
    render partial:
      '/tmt/user_activities/history',
      locals: {
        activities: @test_case.user_activities,
        is_remote: true
      }
  ) %>", "<%= @request_url %>");
} else {
  location.reload();
}
