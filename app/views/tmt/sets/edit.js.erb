app.modal.add("<%= render_modal({
  header: present(@set).header_name,
  body: render(partial: 'form',
    locals: {
      set: @set,
      sets: @sets,
      project: @project,
      parent: @parent,
      url: project_set_path(@project, @set)
    }
  )
}) %>")
$('.messages').replaceWith("<%= j(render(partial: '/layouts/messages'))%>")
app.sets.init().event.findPlaceForDropdownMenu();
app._bootstrap.dropdownMenu();
