app.modal.add("<%= render_modal({
  header: t(:new_project, scope: :projects),
  body: render(partial: "form",
    locals: {
      project: @project,
      url: admin_projects_path(),
      test_case_types: @test_case_types
    }
  )
})%>")
