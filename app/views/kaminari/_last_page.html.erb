<% if current_page.last? %>
  <li class='disabled'>
    <a><%= raw(t 'views.pagination.last') %></a>
  </li>
<% else %>
  <li>
    <%= link_to raw(t 'views.pagination.last'), url, :rel => 'last', :remote => remote %>
  </li>
<% end %>
